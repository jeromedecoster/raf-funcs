{
  "name": "raf-funcs",
  "version": "0.1.0",
  "description": "A very limited subset of requestAnimationFrame functions I use every day",
  "repository": "jeromedecoster/raf-funcs",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server public --no-ui --no-notify",
    "dev": "node scripts/dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.11.2",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  },
  "author": "",
  "license": "MIT"
}
